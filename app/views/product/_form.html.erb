<%= form_for image do |f| %>
  <div>
    <!--%= f.label :name %-->
    <%= f.hidden_field :name %>
  </div>
 
  <div>
    <!--%= f.label :image %-->
    <%= f.file_field :image %>
  </div>
 
  <%= f.submit "Upload Image" %>
<% end %>
