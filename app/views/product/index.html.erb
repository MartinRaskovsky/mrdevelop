<center>
<h3>Product</h3>
  <% type  = @product['type'] %>
  <% brand = @product['brand'] %>
  <% model = @product['model'] %>
  <% image = @product['image'] %>
  <% files = @product['files'] %>
  <% descr = @product['description'] %>
  <table>
  <tr>
    <td width=400>
      <%= brand %>
      <%= model %>
      <br><br><br><br>
      <i><%= descr %></i>
    </td>
    <td>
        <%= image_tag("#{image}") %>
    </td>
  </tr>
  </table>
  <!--% @product.each do |key, value| %>
    <li><b>< %= key %></b> < %= value %></li>
  < % end %-->

<h3>Images</h3>                                                                                                               
  <table>
  <% @images.length.times do |i| %>
    <% img = @images[i].image %>
    <% if i % 8 == 0 %>
      <% if i > 0 %>
    </tr>
      <% end %>

    <tr align=center>
    <% end %>
      <td><%= link_to(image_tag("#{img.thumb.url}", {:alt => "Image"}), "#{img.url}", target: '_blank') %></td>  
  <% end %>
    </tr>
  </table>

<h3>Upload</h3>

<% @image = Image.new %>
<%= render 'form', image: @image %>

</center>
