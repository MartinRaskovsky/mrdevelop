<h1>Product</h1>
  <% type  = @product['type'] %>
  <% brand = @product['brand'] %>
  <% model = @product['model'] %>
  <% image = @product['image'] %>
  <% files = @product['files'] %>
  <% descr = @product['description'] %>
  <table>
  <tr>
    <td width=400>
      <%= brand %>
      <%= model %>
      <br><br><br><br>
      <i><%= descr %></i>
    </td>
    <td>
        <%= image_tag("#{image}") %>
    </td>
  </tr>
  </table>
  <h4><font color="#ff0000">Implement image upload</font></h4>
  <!--% @product.each do |key, value| %>
    <li><b>< %= key %></b> < %= value %></li>
  < % end %-->
